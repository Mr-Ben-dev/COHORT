pragma language_version 0.23;
import CompactStandardLibrary;

export ledger opened: Boolean;

export circuit openIfReady(): [] {
  assert(blockTimeGte(1 as Uint<64>), "too early");
  opened = true;
}
